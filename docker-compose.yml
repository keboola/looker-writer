version: '2'
services:
  dev: &default
    build:
      context: .
    image: looker_writer_v2_dev
    environment:
      - KBC_RUNID
      - KBC_CONFIGID
      - KBC_URL
      - KBC_TOKEN
    volumes:
      - ./:/code
      - ./data:/data

  dev-xdebug:
    <<: *default
    build:
      context: docker/xdebug
